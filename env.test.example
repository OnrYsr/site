# Muse3DStudio Test Environment Variables (Server-based)

# Database (Test database on same server)
DATABASE_URL="postgresql://postgres:@localhost:5432/muse3dstudio_test"

# NextAuth (Server IP with test port)
NEXTAUTH_URL="http://16.171.140.155:3001"
NEXTAUTH_SECRET="muse3dstudio-test-secret-2024"

# App Environment  
NODE_ENV="development"
PORT=3001

# Test specific settings
ENABLE_TEST_DATA="true"
TEST_MODE="true"

# Optional: Email settings (disabled in test)
# SMTP_HOST=""
# SMTP_PORT=""
# SMTP_USER=""
# SMTP_PASS=""

# Optional: File uploads (test path on server)
UPLOAD_PATH="/var/www/muse3dstudio/uploads/test"
MAX_FILE_SIZE="5MB" 